[package]
name = "auto-rapid"
version = "0.1.0"
edition = "2021"

[dependencies]
autocxx = "0.27.1"
cxx = "1.0"

[build-dependencies]
autocxx-build = "0.27.1"
miette = { version = "5", features = ["fancy"] } # optional but gives nicer error messages!

